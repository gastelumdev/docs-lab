export interface TEvent {
    _id?: any | null;
    name: string;
    description: string;
    owner: string | null
}

export interface tEvent {
    id?: string | null;
    name: string;
    description: string;
}